<div class= "cards">
    <% @bookings.each do |booking| %>
    <% if booking.user_id == current_user.id %>
        <div class="card">
            <h3><%=  booking.total_price  %></h3>
            <% else %>
        </div>
      <% end %>
    <% end %>
</div>
